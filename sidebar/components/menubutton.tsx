export default function MenuButton({
    title,
    imgSrc,
}: {
    title: string;
    imgSrc: string;
}) {
    return (
        <button className="py-2 flex justify-between items-center w-full">
        </button>
    )
}
